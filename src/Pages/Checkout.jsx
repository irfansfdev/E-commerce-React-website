import React from "react";
import CheckoutInfo from "../websiteComponents/CheckoutInfo";

// Future: Import Login Check here
const Checkout = () => {
  // You can add logic here later to check: if (!user) return <Login />
  
  return (
    <>
      <CheckoutInfo />
    </>
  );
};

export default Checkout;